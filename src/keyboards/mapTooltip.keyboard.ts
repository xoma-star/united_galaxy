import {InlineKeyboardButton} from "node-telegram-bot-api";

const mapTooltipKeyboard: InlineKeyboardButton[][] = [
    [{text: 'Открыть карту', web_app: {url: 'https://united-galaxy.vercel.app'}}]
]

export default mapTooltipKeyboard